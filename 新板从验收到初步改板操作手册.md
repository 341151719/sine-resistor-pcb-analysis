# C11_35 正弦电阻板：从新板验收到初步改板

**文档用途：** 给第一次拿到板子的工程师一条可以照着执行的路线：先确认板子是什么版本，再在低风险条件下确认数字功能，接着写入并验证固件，最后逐步接通模拟/功率部分，复现已知问题，并用可逆的 A/B 实验决定是否改板。

**适用项目：** `Active Acoustic Impedance Controller C11_35`（本仓库中的“正弦电阻”项目）。

**当前基线：** 推荐新板以 [V8 稳定性修正版](KICAD部分/正弦电阻V8_KiCad10.0.4_稳定性修正版_2026-09-04/) 为检查基线；[V7 加工基线](KICAD部分/给厂商的板子加工数据/正弦电阻V7_KiCad10.0.4_校正转换版/) 只作为历史板和问题复现对照，不要把 V7 的旧参数误当成 V8 参数。

**重要限制：** 当前仓库没有 STM32 的 C/C++ 工程、`.elf`、`.hex` 或 `.bin` 正式发布物。仓库中的实板记录描述了曾经使用的“V2 固件”命令和保护行为，但这些行为目前只能作为实验记录，不能当成可审计的发布固件。写固件一节因此同时说明“应如何实现”和“拿到真实固件后如何验证”。

---

## 0. 先理解整块板子

### 0.1 这块板分成三个电源域

不要把三个电源接口当成普通的“正负电源插座”。它们服务于不同电路，中心地也不同。

| 接口 | 作用 | 建议首次设置 | 地线 | 第一次上电是否接入 |
|---|---|---:|---|---|
| J3 | MCU、UART、3.3 V 数字域 | 5.0 V | DGND | 最先，只接它 |
| J2 | AD633、OPA1656 等模拟前级 | 先 ±8 V，再 ±10 V | AGND | 数字域确认后 |
| J1 | OPA548 功率级对称电源 | 先 ±4 V | PGND | 前级稳定后 |
| J4 | 扬声器/外部负载 | 首次保持开路 | PGND | 最后 |

电源域的名称可以这样记：

- **J3 是“让 MCU 活起来”的电源。** 没有它不能通信和烧录。
- **J2 是“让模拟信号链工作”的电源。** TP7、TP8 的主要前级波形依赖它。
- **J1 是“让 OPA548 有能力输出功率”的电源。** 它同时会通过 R5、OPA548 输入和 E/S 支路反过来影响 TP8。
- **J4 是负载，不是测试点。** 根因排查时先不接扬声器。

AGND、PGND、DGND 在板上有受控连接，不能用外部粗导线随意短接。J2 的中心点接 AGND，J1 的中心点接 PGND，J3 的负端接 DGND。

### 0.2 信号链只需先记住这一条

```text
MCU / DAC → AD633 与模拟注入 → TP7（MUL_RAW）→ U4B/OPA1656 → TP8（VCMD）
                                                               ↓ R5=100 Ω
                                                        OPA548 +IN
```

因此排查 100–110 kHz 异常时，最有用的第一组比较是 **TP7 和 TP8 同时测量**：

- TP7 干净、TP8 异常，说明问题第一次明显出现在 U4B 输出及其后级负载一带；
- TP7、TP8 同时异常，才需要把搜索范围向前扩大到 AD633、DAC、模拟电源或地参考；
- 仅凭“OPA ON 后电流变大”不能证明振荡由 OPA548 首先产生。

### 0.3 新板应该使用哪个版本

V8 相比 V7 已经做了几项重要修正：

- R12 恢复为量产的 4.7 Ω / 2 W 封装；
- C27 从 10 pF 改为 10 nF；
- R10 从 100 kΩ 改为 6.8 kΩ，以增加 OPA548 E/S 关断裕量；
- C16 移到 U4 负电源脚附近；
- TP1–TP20 改为便于接线的镀通孔测试点；
- J7 改为 1.27 mm、5 针 SWD 接口。

这些修改降低了已知风险，但**尚未证明已经消除了实板的 100–110 kHz 问题**。V8 仍需按本文做实板验收。

### 0.4 接口和测试点对照表

第一次接线时只看下面这张表，不要凭“左边是正、右边是负”猜针脚。接线后仍要在断电状态用导通关系复核。

| 接口 | Pin 1 | Pin 2 | Pin 3 | 其他 |
|---|---|---|---|---|
| J1 | PWR_P | PGND | PWR_N | OPA548 对称电源 |
| J2 | ANA_P_EXT | AGND | ANA_N_EXT | 模拟前级对称电源 |
| J3 | V5_IN | DGND | — | MCU/数字 5 V 输入 |
| J4 | SPK | PGND | — | 扬声器或假负载；排查时开路 |
| J5 | VK_MCU | VK | VK_EXT | MCU 控制用 1–2；外部 VK 用 2–3；Pin 2 不悬空 |
| J7 | V3V3 | SWDIO | SWCLK | Pin 4=NRST，Pin 5=DGND |

常用测试点：

| 测试点 | 网络 | 主要用途 | 参考地 |
|---|---|---|---|
| TP7 | MUL_RAW | U4B 输入侧 | TP16 / AGND |
| TP8 | VCMD | U4B 输出、R5 前端 | TP16 / AGND |
| TP9 | OPA_ES | OPA548 E/S | 与 TP14 做差分或 DMM |
| TP11 | ANA_P | U4 实际正模拟轨 | TP16 / AGND |
| TP12 | ANA_N | U4 实际负模拟轨 | 差分，禁止地夹直接接这里 |
| TP13 | PWR_P | OPA548 正轨 | TP17 / PGND |
| TP14 | PWR_N | OPA548 负轨 | 差分/DMM，禁止普通地夹 |
| TP16 | AGND | 模拟信号参考 | — |
| TP17 | PGND | 功率测量参考 | — |
| TP1 | SPK | 功率输出侧 | TP17 / PGND |
| TP2 | DRV | OPA548 输出 | TP17 / PGND |
| TP15 | V3V3 | MCU 3.3 V | DGND |

V8 测试点为便于重复接线的镀通孔，但仍应使用短线和应力释放。V7 或未知版本的测试点可能是小型 SMD 裸焊盘，禁止通电时用手持表笔长时间压住。

---

## 1. 收到新板后的第一小时：不要急着上电

### 1.1 准备一个 Board ID

每块板都分配一个不会重复的编号，例如：

```text
C11-35-V8-20260915-A01
```

把下面内容放进一个单独的实验目录，例如：

```text
records/C11-35-V8-20260915-A01/
├── 00_package.jpg
├── 01_top_before_power.jpg
├── 02_bottom_before_power.jpg
├── 03_u1_u4_marking.jpg
├── 04_swd_j7.jpg
├── board_record.md
└── waveforms/
```

第一次记录至少包括：

| 要记录的东西 | 为什么 |
|---|---|
| 板号、生产日期、厂家订单号 | 后面出现差异时能追溯批次 |
| V7 还是 V8 | R10、C27、测试点形式不同 |
| PCB 顶面、底面全景 | 留下焊桥、缺件、刮伤的证据 |
| U1、U4、U5 顶标 | 确认器件型号和方向，避免“换过但没记录” |
| R5、R10、R12、R15、R16、C27、C39 等局部照片 | 这些位置会参与已知问题或改板 |
| J5 桥接状态、J7 是否装排针 | 这两个状态会改变启动和控制行为 |

不要只拍“看起来正常”的照片。照片应该能看清丝印、器件顶标、焊点和连接器方向。

### 1.2 先确认板版本

按下面顺序判断：

1. 查看订单或生产资料是否明确写了 V8。
2. 打开 V8 的 [RELEASE_NOTES_V8.md](KICAD部分/正弦电阻V8_KiCad10.0.4_稳定性修正版_2026-09-04/RELEASE_NOTES_V8.md)，对照 R10、R12、C27 和 J7。
3. 断电，用放大镜确认板上的 R10 标称值；V7 常见为 100 kΩ，V8 目标为 6.8 kΩ。
4. 如果仍然无法区分，把它标成 `UNKNOWN`，按更保守的 V7 风险流程处理；不要因为板子上有“V8”字样就跳过实测。

### 1.3 对照 BOM，不要只数元件

V8 工程的受控文件在 `KICAD部分/正弦电阻V8_KiCad10.0.4_稳定性修正版_2026-09-04/audit/` 和 `release/V8_2026-09-04/assembly/`。检查时优先看：

- `BOM_SMT.csv`：实际贴装的 73 个 SMT 位号；
- `BOM_dnp_44.csv`：DNP（设计上不装）的 44 个位号；
- `BOM_post_2.csv`：后焊件；
- `CPL_SMT.csv`：贴片坐标和面别。

重点不是把每个电容都重新测一遍，而是确认以下项目没有被装错、漏装或意外桥接：

| 位号/接口 | V8 期待状态 | 检查理由 |
|---|---|---|
| R5 | 100 Ω，VCMD 到 OPA548 +IN | 后面 R5 隔离实验的关键元件 |
| R10 | 6.8 kΩ | OPA548 E/S 关断下拉 |
| R12 | 4.7 Ω / 2 W，2512 | OPA548 输出阻尼/功率路径 |
| R15、R16 | 约 1 kΩ、39 kΩ | U4B 反馈比例约 1/40 的基础 |
| C27 | 10 nF | OPA548 两电源之间的去耦 |
| C14、C37–C41 | 按 BOM 标为 DNP 时不应装件 | 这些位置用于后续补偿诊断，不应被误当成正式补偿 |
| J5 | 使用 MCU 控制时跨 1–2；外部 VK 时跨 2–3；J5-2 不悬空 | VK 来源选择错误会让后续波形失去意义 |
| J7 | 5 针 SWD；V8 建议装匹配排针或 pogo 夹具 | 裸焊盘上手压散线会造成烧录不稳定 |
| J4 | 初始开路 | 防止功率级故障直接损坏扬声器 |

如果发现一个“应该 DNP 的位置装了电容”，先拍照和记录，不要通电后再猜它是否有影响。

---

## 2. 断电检查：建立每块板自己的基线

这一节的目标不是证明电路一定正确，而是先排除“上电就会烧”的明显问题。

### 2.1 断电条件

1. 拔掉 J1、J2、J3、J4 的全部电源和负载线。
2. 等待电容放电；用 DMM 确认 TP11、TP12、TP13、TP14 相对各自参考地低于 0.2 V。
3. 所有临时电阻、短接线、示波器探头都先拿掉。
4. 进行电阻或蜂鸣档测量时，先确认 DMM 不在电流档。

### 2.2 先看，再量

按“从外到内”的顺序检查：

1. 连接器是否歪斜、缺针、焊点虚焊。
2. U1（OPA548）、U4（OPA1656）和 U5（STM32G431CBT6）方向是否与丝印一致。
3. 大电容、R12、功率器件的焊盘是否有连锡。
4. R5、R10、R15、R16 是否有明显错值标记。
5. DNP 焊盘之间是否被锡珠连接。
6. J1、J2、J3 中心点是否被外部导线错误共地。
7. TP7、TP8 周围是否有返修残留、刮伤或锡桥。

### 2.3 只做“相对比较”的电阻测量

电容会让 DMM 的读数随时间变化，因此不要规定一个“全板电阻必须是 X Ω”。更可靠的做法是把本板读数与同批未改板比较。

至少测以下几组：

| 测量 | 用途 |
|---|---|
| J3 的 V5_IN–DGND | 发现数字电源硬短路 |
| J2 的 ANA_P_EXT–AGND、ANA_N_EXT–AGND | 发现模拟电源极性/焊桥问题 |
| J1 的 PWR_P–PGND、PWR_N–PGND | 发现功率电源硬短路 |
| J1 正负轨之间 | 与同批板比较，不把电容充电瞬态当成故障 |
| R5 两端到 TP8/OPA548 +IN | 确认 R5 两端网络，不凭 PCB 方向猜 |
| TP9 到 TP14 的直流路径 | 了解 E/S 下拉网络是否存在 |
| J7 每一针到对应网络 | 确认 SWD 焊盘没有被污染或桥接 |

如果出现近似 0 Ω 的电源硬短路，先停在这里。不要用“限流上电试试”代替找焊桥。

### 2.4 给测试点焊短线

V8 的测试点是为了方便重复测量，但仍建议在断电时焊好短线，并在焊点后 5–10 mm 处固定，避免测试时拉掉焊盘。

优先准备：

- TP7、TP8、TP16：观察模拟信号；
- TP9、TP14、TP17：检查 E/S 与功率参考；
- TP11、TP12：检查 U4 实际模拟电源纹波；
- TP13：必要时观察 OPA548 正轨；
- U4B_N：没有独立 TP，优先从 DNP C14 与 TP8 的两侧通过蜂鸣档确认后再引线。

**千万不要凭“左边焊盘/右边焊盘”猜 C14 的网络。** 断电用导通关系确认哪一侧是 VCMD，另一侧才是 U4B_N。

---

## 3. 仪器接线和安全边界

### 3.1 最低设备

- 三组可读电压和电流的电源：J3 5 V、J2 对称模拟电源、J1 对称功率电源；
- 两通道或四通道示波器，优先 10×探头、短地弹簧或差分探头；
- 浮地电池式 DMM；
- ST-Link 或兼容 SWD 调试器；
- USB-UART（如果固件通过串口提供控制）；
- 放大镜、ESD 防护、绝缘细线；
- 返修时使用合格热风/烙铁和助焊剂清洗工具。

### 3.2 示波器地线规则

普通台式示波器的 BNC 外壳和各通道地夹通常都连接保护地。地夹不是“随便找一个负端”。

| 要看的信号 | 参考点 |
|---|---|
| TP7、TP8、TP11、TP12 | TP16 / AGND |
| TP1、TP2、TP13、TP14 | TP17 / PGND；涉及负轨时优先差分测量 |
| TP9−TP14 | 浮地 DMM；或差分探头；或两个通道都对 TP17 后做数学相减 |

禁止把普通示波器地夹接到：

- TP8；
- TP12 / ANA_N；
- TP14 / PWR_N；
- TP2 或任何不是明确参考地的信号点。

项目历史上确实发生过把地夹接到 TP8 的测量。它等效于把 U4B 输出强行拉到地，会造成额外电流和失真的波形；那组数据必须作废。

### 3.3 保守限流表

以下是排障保护值，不是产品额定值。

| 电源 | 第一次设置 | 排障时最多建议 | 立即停止 |
|---|---:|---:|---|
| J3 | 5.0 V，50 mA | 确认无短路后 100 mA；确有需要才到 200 mA | 3.3 V 异常、器件发热、持续 CC |
| J2 | ±8.0 V，每轨 30 mA | 健康基线后 ±10 V、每轨 35–50 mA | 任一轨 CC 超过 0.2 s、两轨相差 >15 mA、TP8 撞轨 |
| J1，OPA OFF | ±4.0 V，每轨 20 mA | 扫描时每轨 30 mA | 任一轨 CC、比基线增加 >10 mA、温升 |
| J1，OPA ON | 只在最后阶段，±4.0 V，每轨 25 mA | 首次正常脉冲后 40 mA；本轮不建议超过 60 mA | 高频输出 >1 Vpp、CC、异常声响或温升 |

如果电源只显示正负轨合计电流，就不要伪装成“已经测到了正负轨不平衡”。必须使用可以分别读出两轨的通道或串联电流测量。

### 3.4 停机顺序

正常停机：

1. 先关闭 J2；
2. 再关闭 J1；
3. 最后关闭 J3；
4. 等待各轨对自己的参考地低于 0.2 V 后才能碰板或焊接。

如果 J1 已经进入 CC 或功率级异常：优先立即关闭 J1，紧接着关闭 J2。不能用螺丝刀短接放电；需要加速放电时，使用至少 1 W 的 10 kΩ 绝缘电阻逐轨放电。

---

## 4. 基本测试第一阶段：只给数字域上电

### 4.1 目标

这一阶段要回答三个简单问题：

1. MCU 的 3.3 V 是否稳定？
2. SWD 能否稳定连接和读写？
3. UART、DAC 控制和基本状态机是否工作？

模拟和功率电源全部断开，所以这一阶段不能被 TP8 振荡影响。也正因为如此，如果此阶段烧录仍失败，应优先查 BOOT、SWD 接触、NRST 和 3.3 V，而不是先猜模拟故障。

### 4.2 接线

- J1、J2、J4：全部断开；
- J3：接 5.0 V，限流 50 mA；
- J7：使用固定排针或定位夹具，不要用五根手线压在裸焊盘上。

当前 V8 J7 网络顺序为：

| J7 针脚 | 网络 | 用途 |
|---:|---|---|
| 1 | V3V3 | 调试器检测目标板电压，不让 ST-Link 反向给板供电 |
| 2 | SWDIO | 数据 |
| 3 | SWCLK | 时钟 |
| 4 | NRST | 复位，建议连接 |
| 5 | DGND | 调试参考地 |

### 4.3 上电后检查

1. 用 DMM 测 TP15 对 DGND，确认约 3.3 V 且不持续下降。
2. 观察 J3 是否快速进入 CC；如果进入，立即断电检查，不要提高限流“硬顶过去”。
3. 让调试器以 1 MHz 连接，优先使用 `connect-under-reset`。
4. 读取并保存芯片 ID、Flash 大小、OPTR 和连接日志。

V8 设计中 PB8 与 BOOT0/I2C_SCL 共用，没有额外硬件强下拉。生产编程要求写入并回读正确的启动选项字节：

```text
期望：0xFBEFF8AA
历史异常：0xFFEFF8AA  （会进入系统 ROM，而不是用户 Flash）
```

不同版本的 STM32CubeProgrammer 对选项字节显示方式可能不同。不要把一个界面显示的字段值直接当成另一个版本的 32 位原始值；应在同一工具中完成“读取 → 写入 → 重新读取”闭环，并把原始日志保存下来。

### 4.4 基本烧录可靠性测试

在真正接模拟电源以前，连续做 10 次：

```text
断电 5 秒 → 上电 → SWD 连接 → 读 OPTR → 擦除 → 写入 → Verify → 复位运行
```

记录每次是否成功、工具错误原文、J3 电压/电流和 NRST 状态。1 MHz 的 10/10 通过之后，才有意义做 4 MHz 作为速度裕量对照。

---

## 5. 写固件：先写“安全状态机”，再写波形

### 5.1 当前仓库的固件事实

SPICE 验证报告已经明确记载：提供的资料没有 MCU 源码，因此实际固件状态是 `not_auditable`。当前仓库也找不到正式 `.hex/.bin` 发布物。

这意味着：

- 不能从本仓库“下载一个确定的 V2 固件”；
- 不能把历史实验记录里的串口命令当成固件 ABI 的永久保证；
- 不能只凭 `ADC=OK` 说模拟反馈链已经正确，因为 BOM 中几路主要 ADC 前端仍可能是 DNP；
- 每次拿到外部固件，都必须记录来源、版本、构建提交和 SHA-256。

当前 KiCad V8 BOM 的 MCU 位号是 **U5 = STM32G431CBT6**。旧 SPICE 文字中偶尔使用 U6 作为 MCU 位号；遇到冲突时以当前原理图/PCB 位号为准，不要按旧文字改线。

### 5.2 固件最小安全合同

在实现任何正弦波、阻抗算法或功率输出之前，固件至少应满足下表：

| 时刻/状态 | 必须行为 | 为什么 |
|---|---|---|
| 上电、复位、看门狗复位 | OPA548 保持 OFF；DAC 输出置零或已知安全值 | 防止 MCU 尚未初始化就打开功率级 |
| UART 命令解析错误 | 回复错误，不改变功率状态 | 防止乱码变成 OPA ON |
| 修改 DAC 目标 | 自动回到 OPA OFF | 历史 V2 已采用的安全策略 |
| OPA ON 超时 | 约 5 s 没有有效命令自动 OFF | 防止通信断开后持续输出；实验时仍不能依赖它替代人工停机 |
| 电流、输出、温度异常 | 立即关 OPA，锁存故障并报告 | 保护逻辑不能只看一个瞬时采样 |
| 目标电阻越界或变化过快 | 拒绝命令或限制斜率 | 防止一条串口命令直接把系统推入未知工作点 |

第一版固件应当是“静态零命令 + 可控 DAC + 可读状态”，而不是一上来运行完整的闭环算法。

### 5.3 建议的实现顺序

#### 第一步：固定引脚和时钟

从 KiCad 原理图确认：

- UART TX/RX 的实际网络；
- DAC1、DAC2 对应的 MCU 引脚；
- OPA ON/OFF 控制输出和复位默认电平；
- ADC 输入、DMA 触发定时器；
- SWD、NRST、BOOT0 共用引脚的限制。

把引脚表作为固件仓库中的受控文件，和原理图 commit 一起保存。不要根据 PCB 上的“附近焊盘”猜 MCU 引脚。

#### 第二步：只做初始化和心跳

上电后先完成：

1. 时钟和电源监控；
2. GPIO 安全默认值；
3. UART 接收；
4. 定时器心跳；
5. 状态查询。

此时 OPA 仍必须 OFF，DAC 可以输出中点/零点。先用 J3 单独上电验证，不接 J1/J2。

#### 第三步：加入 DAC1，再加入 DAC2

一次只打开一个 DAC 通道：

1. 写入固定码；
2. 用示波器观察对应测试点；
3. 保存“码值—实测电压”表；
4. 再加入下一通道。

历史记录中 DAC1 用 `250 → 501 → 750` 观察 TP6/VK，结果为“负电压 → 接近 0 → 正电压”。DAC2 在 TP5 看到变化只能证明控制路径有响应；TP5 中间经过模拟注入和 U4A，不能当作 DAC2 直接精密标定。

#### 第四步：加入 ADC/DMA，但不要过度解读

让 ADC/DMA 产生递增帧计数，并通过 UART 报告 `FRAMES`。这只能证明定时器、ADC、DMA 的数字链在运行。若 BOM 中 ADC 前端是 DNP，必须在报告里写明“数字采样链通过，模拟反馈未签核”。

#### 第五步：最后才加入 OPA 状态机

建议状态：

```text
BOOT_SAFE → DIGITAL_IDLE → ANALOG_READY → OPA_ARMED → OPA_ON
                                  ↑             ↓
                              FAULT_LATCH ← 超时/异常
```

- `BOOT_SAFE`：只初始化，不允许 ON；
- `DIGITAL_IDLE`：UART、DAC、状态查询可用；
- `ANALOG_READY`：J2 供电和前级由人工确认稳定；
- `OPA_ARMED`：满足温度、电流、TP8 稳定等条件后，允许短脉冲；
- `OPA_ON`：需要 watchdog、限时和故障锁存；
- `FAULT_LATCH`：必须明确报告故障，重新上电或人工清除才可恢复。

固件不应通过普通 UART 命令直接绕过 `OPA_ARMED`。实验室调试可以有特殊命令，但应编译为独立 debug 版本并明确标记。

### 5.4 UART 命令兼容层

历史 V2 实验记录中使用过以下命令：

```text
PING / HELP / STATUS / READ ADC / STREAM / SET DAC1 / SET DAC2 / OPA ON / OPA OFF
```

实现新固件时建议保留这些命令的可读性，但在 `HELP` 中显示固件版本、构建提交和状态约束。特别是：

- `SET DAC1`、`SET DAC2` 成功后应明确回复“OPA forced OFF”；
- `OPA ON` 回复实际进入的是 `ARMED`、`ON` 还是拒绝；
- `STATUS` 至少包含 OPA 状态、DAC 码、故障锁存、帧计数和 watchdog 剩余时间；
- 每条命令都带结束符和错误回复，避免半条命令改变功率状态。

不要在文档中猜 UART 波特率。波特率应由真实固件源码或发布说明决定，并写入实验记录。

### 5.5 构建和发布固件

每一次可以烧录的固件都要同时保存：

```text
firmware_version: v0.1.0
source_commit: <完整 Git SHA>
toolchain: STM32CubeIDE/CMake + 版本号
artifact: c11_35_v0.1.0.hex
sha256: <完整哈希>
build_flags: <完整参数>
option_bytes: 0xFBEFF8AA
```

至少做三类检查：

1. **主机侧检查：** 命令解析、超时、边界值、拒绝非法 OPA ON；
2. **J3 单独上电：** UART、DAC、ADC/DMA、watchdog；
3. **接模拟电源但 OPA OFF：** 状态机不会意外打开功率级。

只有这三类检查都通过，才把固件拿到下一阶段。

---

## 6. 烧录固件：安全、可重复、可回退

### 6.1 烧录前条件

- J1、J2、J4 全部断开；
- J3 接 5 V，限流 50–100 mA；
- J7 使用固定连接器；
- ST-Link 不向目标板反向供电，只读取 V3V3；
- 记录目标板 Board ID 和待写入固件 SHA-256；
- 记录当前 OPTR，不能在“没保存原值”的情况下盲改。

### 6.2 使用 STM32CubeProgrammer 的推荐步骤

1. 启动 STM32CubeProgrammer，选择 **SWD**。
2. 连接频率设为 **1 MHz**。
3. 勾选或选择 **connect under reset**，连接 NRST。
4. 连接 J7 的 V3V3、SWDIO、SWCLK、NRST、DGND。
5. 连接目标板后，先点 Read，保存芯片 ID、Flash 信息和 Option Bytes 截图。
6. 选择正确的 `.hex` 或 `.bin`；如果是 `.bin`，必须确认链接地址，不能直接猜 `0x08000000` 以外的地址。
7. 执行擦除、写入、Verify、复位；保存完整日志。
8. 再次读取 OPTR，确认仍为 `0xFBEFF8AA` 的预期启动配置。
9. 断开 SWD 后只给 J3 上电，先做 UART `PING` 和 `STATUS`。

### 6.3 CLI 示例

不同版本的 STM32CubeProgrammer CLI 参数略有差异，下面只给出便于核对的示例；执行前先运行本机版本的 `--help`，并保存命令行输出：

```bash
STM32_Programmer_CLI -c port=SWD freq=1000 mode=UR
STM32_Programmer_CLI -c port=SWD freq=1000 mode=UR -ob displ
STM32_Programmer_CLI -c port=SWD freq=1000 mode=UR \
  -w c11_35_v0.1.0.hex -v -rst
```

不要把选项字节的原始 32 位值直接拼成未经验证的 CLI 写入参数。先确认工具版本、芯片系列字段名和当前值，再写入并回读。`0xFFEFF8AA` 进入系统 ROM 的历史问题必须用回读证据确认已经消失。

### 6.4 烧录后最小验收

只给 J3 上电，依次执行：

```text
PING
HELP
STATUS
READ ADC
STREAM（短时间）
SET DAC1 250
SET DAC1 501
SET DAC1 750
SET DAC2 <一个安全固定码>
OPA OFF
```

预期：

- `PING` 有稳定回复；
- `STATUS` 显示 OPA 为 OFF；
- `STREAM` 或 `FRAMES` 递增，但不把它当作模拟反馈已经正确的证据；
- DAC1 改变时 TP6/VK 按“负 → 近零 → 正”变化；
- 修改 DAC 后 OPA 仍保持 OFF；
- J3 不进入 CC，MCU 不异常发热。

任何一步失败都先回到数字域，不要为了“继续看模拟波形”接 J1/J2。

### 6.5 烧录失败排查表

| 现象 | 先检查 | 不要先做什么 |
|---|---|---|
| 找不到目标 | J7 针脚、DGND、V3V3、NRST、SWD 频率 | 不要提高 J3 限流硬试 |
| 只能偶尔连接 | 固定夹具、1 MHz、connect-under-reset、NRST 波形 | 不要一边手压排线一边写入 |
| 写入成功但复位不运行 | OPTR、BOOT0/PB8、Flash 地址、NRST | 不要把它归因于模拟振荡 |
| J3 立刻 CC | 断电查 3.3 V 短路、U5 方向、焊桥 | 不要反复上电 |
| 只在接 J1/J2 后通信失败 | 先重新断开 J1/J2 做数字域 10/10 | 不要把 J1/J2 和固件问题同时改 |

---

## 7. 基本模拟测试：保持 OPA OFF

### 7.1 上电顺序

确认 J3 数字测试通过后：

1. J3 5 V 上电，确认固件运行且 OPA 命令为 OFF。
2. J1 设为 ±4.0 V，每轨 20 mA；先给 OPA548 供电但保持 OFF。
3. J2 先设 ±8.0 V，每轨 30 mA，确认极性和电流正常。
4. 再把 J2 提到 ±10.0 V，每轨约 35 mA。
5. 观察 TP7、TP8、TP11、TP12，并记录 J1/J2 正负轨电流。

先给 J1 再给 J2，是为了避免 TP8 通过 R5 驱动一个完全无电的 OPA548 输入。J4 仍保持开路。

### 7.2 观察什么

建议示波器设置：

- TP7 对 TP16；
- TP8 对 TP16；
- 10×探头；
- 先保存未开带宽限制的原始波形，再保存 20 MHz 限制波形；
- 用 2–5 µs/div 看 100–110 kHz 周期，用 100–500 µs 窗口看是否为持续振荡或偶发包络。

健康基线的最低要求是：

- TP7 没有明显撞轨；
- TP8 在至少 10 s 观察内没有持续 100–110 kHz 大波形；
- TP8 高频分量小于约 50 mVpp，且直流工作点没有贴轨；
- J1/J2 不进入 CC，正负轨没有明显失衡；
- OPA 命令始终 OFF。

这些是进入 OPA ON 前的排障门槛，不是产品最终性能指标。

### 7.3 E/S 的正确检查方式

用浮地 DMM 测 `TP9 - TP14`，记录每一个 J1 电压档的读数。OPA548 数据手册的状态边界是：

- `E/S ≤ V− + 0.8 V`：明确 Disable；
- `E/S ≥ V− + 2.4 V`：明确 Enable；
- 中间区域：状态不保证。

DMM 只能告诉我们慢速或平均值，不能证明 E/S 在 105 kHz 的每一个周期都没有跨过门限。若要看最小/最大或交流分量，使用差分探头，或两个通道都对 TP17 测量后数学相减。

---

## 8. 复现已经发现的问题

复现问题的原则是：**先复现低风险问题，再复现高风险问题；一次只改变一个变量；明确“复现了什么”和“没有复现什么”。**

### 8.1 问题 A：V7 的 R10=100 kΩ 关断裕量不足

这是已经有定量 SPICE 和实板证据的问题。V7 的 R10=100 kΩ 使 OPA548 E/S 下拉太弱；并联 10 kΩ 后等效约 9.1 kΩ，历史实测 `TP9−TP14` 约 0.55–0.65 V，SPICE 也得到约 0.614 V。V8 已改为 6.8 kΩ，目标 SPICE 值约 0.463 V。

**不要为了复现问题把一块新 V8 永久改回 100 kΩ。** 有 V7 板时直接测 V7；没有时只做断电、可恢复的临时电阻对照。

安全步骤：

1. J4 开路，OPA 命令 OFF。
2. J3、J2 按前面的顺序上电，J1 从 ±4 V 开始。
3. 每个 J1 档位记录 `TP9−TP14`、TP8 波形和电流。
4. 只在断电后把 10 kΩ 电阻焊接在 TP9 与 TP14 之间，等效并联原 R10；不要通电手持电阻。
5. 重复完全相同的 J1 扫描。
6. 断电拆掉临时电阻，再重复一次原条件。

判定：

- 并联后 E/S 进入明确 Disable 区、振荡可逆消失：说明 E/S 偏置是必要参与因素；
- 并联后只改变波形、不消失：E/S 参与但不是唯一原因；
- 没有变化：R10 不是当前主因，但不能由单次 DMM 静态值排除高频动态耦合。

### 8.2 问题 B：OPA OFF 时 TP7 干净、TP8 出现 100–110 kHz

这是目前最重要、但尚未由健康 SPICE 模型自然复现的实板现象。

复现条件：

- J4 开路；
- OPA 命令保持 OFF；
- J2 约 ±10 V；
- J1 从 ±4 V 开始逐级扫到 ±6.5 V；
- TP7、TP8 同时以 TP16/AGND 为参考；
- 不把示波器地夹接到 TP8、TP12 或 TP14。

步骤：

1. 在 ±4.0 V 记录 10 s 基线。
2. 以 ±4.5、±5.0、±5.5、±6.0、±6.5 V 逐档变化 J1；J2 保持不变。
3. 每档最多停留 3 s；一旦 TP8 出现超过 1 Vpp 的持续高频，只保存一次同步波形并在 0.2 s 内关断。
4. 记录 TP7/TP8 的 Vpp、频率、DC、相位关系，以及 J1 正负轨电流。
5. 记录 J1 接近 ±6 V 时，TP8 是否从规则波形变成不规则纹波。

有价值的复现结果是：

```text
OPA 命令 OFF
TP7 基本干净
TP8 出现约 100–110 kHz 的大幅规则波形
改变 J1 后频率/波形形态可重复变化
```

这支持 J1/OPA548 支路参与，但**不单独证明 OPA548 是振荡起点**。要判定“OPA548 输入支路是不是必要条件”，下一步必须做 R5 断开—恢复 A-B-A。

### 8.3 问题 C：OPA ON 后 J1 进入约 70 mA CC

历史上 `OPA ON → J1 约 70 mA CC → watchdog 或 OPA OFF 后恢复` 的关联是可重复的；但当前证据还不能把它写成“已经证明的持续 70 mA 电流机制”。

**不要在 TP8 尚未稳定时主动执行 OPA ON。** 只有满足以下条件才允许短脉冲验证：

- TP8 连续 10 s 没有 100–110 kHz；
- TP8 高频小于约 50 mVpp；
- J1/J2 没有 CC；
- J4 已换成 1 kΩ、至少 2 W 的纯电阻假负载，而不是扬声器；
- J1 设 ±4 V，每轨限流 25 mA；
- 示波器同时测 TP8/TP16 和 TP2/TP17。

验证步骤：

1. 固件输出静态零命令。
2. 执行 OPA ON，持续 20–50 ms 后立即 OPA OFF。
3. 连续三次正常才可以延长到 100 ms、500 ms、1 s；每一级保存波形和正负轨电流。
4. 如果出现 TP2 高频超过 1 Vpp、TP8 高频超过 50 mVpp、J1 接近限流或器件升温，立即 OFF 并停机。

记录时必须分开写：

- 电源显示的持续/平均电流；
- 示波器或仿真得到的瞬时峰值；
- J1 正轨、负轨还是 J2 电流。

旧仿真在 2 Vpk/105 kHz 条件下得到约 69 mA 峰值，但正负轨平均约 23.6/23.5 mA，而且理想模型没有台式电源限流环路。因此“69 mA 峰值”不能直接等同“持续 70 mA CC”。

### 8.4 问题 D：示波器地夹接 TP8 的测量伪影

这个问题**不应该为了复现而在实板上故意重做**。它的等效 SPICE 复现已经足够说明机制：TP8 会被近似短接到地，U4B 输出级被强制重载，电流可能从几 mA 增到几十 mA甚至更高。

历史记录中“探头接上后 J2 从约 20 mA 增到约 70 mA”的那一轮数据不能用于判断板子本身故障。以后每次上电前，让第二个人按清单复核所有示波器地夹位置。

### 8.5 用仓库里的 SPICE 案例做先验检查

进入仓库根目录后，可运行：

```bash
bash 实验还原部分/早期错误复现/run_all.sh
python3 实验还原部分/主动审计/reassess_evidence.py
bash 实验还原部分/主动审计/run_checks.sh
```

这些脚本的意义不同：

| 脚本 | 用途 | 当前能说明什么 |
|---|---|---|
| `早期错误复现/run_all.sh` | 复现 R10、错误地夹、OPA ON 高频负载后果和健康基线 | 能复现已知测量/参数后果，但健康模型仍不能自然产生目标 105 kHz |
| `主动审计/reassess_evidence.py` | 检查峰值/平均电流、105 kHz AC 和较大启动扰动 | 诊断案例通过，实板根因仍未确定 |
| `主动审计/run_checks.sh` | 检查 H07/H08 deck 是否真的跑完所有案例 | H07/H08 固定偏置案例完整，但不覆盖真实上电顺序 |

SPICE 的“没有自然振荡”是一个阴性结果，不是程序失败。不能为了让仿真出现 105 kHz 而人为注入目标频率，再把它当成健康电路的根因复现。

---

## 9. 初步改板：先做可逆 A/B，再决定永久改动

### 9.1 改板总原则

每次改板前后都要保留：

1. 改板前的照片、阻值、供电设置和 TP7/TP8 同步波形；
2. 只改变一个变量；
3. 改板后的同一套条件和数据；
4. 如能恢复原状态，必须恢复后再测一次；
5. 至少在第二块板重复，才能把结果从“相关”提高到“较强因果证据”。

改板必须在断电、放电并确认所有轨低于 0.2 V 后进行。不能通电夹电阻、按焊盘、移动探头或焊接。

### 9.2 第一优先：R5 断开—恢复 A-B-A

R5 是 TP8 到 OPA548 +IN 的隔离电阻，也是当前信息量最高的改动。

#### A：原始状态

1. 按第 8.2 节重现 TP8 现象。
2. 保存 TP7、TP8、J1/J2 电流、J1 档位和固件状态。

#### B：拆除 R5

1. 断开所有电源并放电。
2. 完整拆下 R5=100 Ω，不切断 PCB 走线。
3. 用蜂鸣档确认一个焊盘与 TP8 导通；两个焊盘之间应开路。
4. J4 保持开路、OPA OFF。
5. 先只给 J3、J2 上电，J1 物理断开，观察 TP7/TP8 10 s。
6. 再给 J1 从 ±4 V 逐档上电，重复观察。

#### A：恢复 R5

1. 断电、放电后恢复同规格 100 Ω R5。
2. 完全重复原来的 J1 档位、限流、固件和探头条件。
3. 在第二块板重复一次。

判定表：

| 拆 R5 | 恢复 R5 | 结论 |
|---|---|---|
| 仍以相同频率/幅度振荡 | 仍振荡 | OPA548 输入支路不是必要条件，转向 U4/反馈/ANA 供电 |
| 稳定 | 再次振荡 | R5/OPA548 支路是必要条件的强证据 |
| 稳定 | 恢复后仍稳定 | 不能判因，返修热应力或接触变化可能是混杂变量 |
| 频率/幅度改变但未消失 | 恢复后可逆变化 | 支路参与，但不是唯一振荡源 |

### 9.3 第二优先：R10 临时加强下拉

只有在 R5 结果支持 OPA548 支路参与后，才做这项。

1. 断电并放电。
2. 用带绝缘短引线的 10 kΩ 电阻跨接 TP9 与 TP14；它与 V7 的 100 kΩ R10 并联后约为 9.1 kΩ。
3. 保持 OPA 命令 OFF，不执行 OPA ON。
4. 重复原 J1 扫描，记录 TP9−TP14、TP7、TP8 和 J1 电流。
5. 断电拆除 10 kΩ，恢复原条件再测。

如果临时并联可逆消振，只能说明 E/S 偏置或动态是必要参与因素；不能直接决定永久值是 4.7 kΩ、6.8 kΩ 还是其他值。永久值必须同时验证 OFF 裕量和 ON 裕量。V8 已选 6.8 kΩ，但这仍不是 105 kHz 根因已经被证明的意思。

### 9.4 第三优先：补偿电容只作为诊断

如果 R5 拆除后仍振荡，再考虑 C39=22 pF 或 C40=47 pF 作为可逆诊断：

1. 断电、放电，确认 Cfb 原本是 DNP。
2. 只安装一种 C0G/NP0 电容。
3. 重复相同的 TP7/TP8 观察。
4. 拆回电容，确认原振荡是否恢复。

“加电容后不振”只证明它降低了高频环路增益，不能直接证明原因是“缺少这颗电容”。不能在没有 R5 隔离、供电同步波形和换件 A/B 证据时冻结 22/47/220 pF 的量产值。

### 9.5 第四优先：U4 可逆换件

只有 R5 拆除后仍振，且 ANA 轨没有足够大的同步纹波时，才安排 U4/OPA1656 A-B-A：

1. 选择症状稳定的板，保存完整基线。
2. 由有 SOIC 返修经验的人更换来源可追溯的 OPA1656IDR。
3. 不同时改 R5、R10、R15、R16、去耦或 Cfb。
4. 清洗、显微镜检查后重复第 8.2 节。
5. 如果条件允许，把旧 U4 装到测试载板或原板复测，以区分芯片问题和重焊造成的接触变化。

“换完后好了”只有在同一条件、第二块板或旧件复装也能重复时，才足以把器件异常列为高可信根因。

### 9.6 设计级 V8 改动与“初步改板”的区别

如果目标是重新制板，而不是给现有 V7 临时飞线，优先使用仓库内 V8 工程和完整 release 输出，不要在 V7 Gerber 上手工改一个电阻就称为 V8。V8 的改动包括 R12、C27、R10、C16、测试点和 J7，且有对应 BOM/CPL/DRC/装配交叉检查。

重新制板前仍要保留两个判断：

- V8 的 R10、C27 等修改是设计风险修正；
- 它们不是已经证明的 100–110 kHz 唯一根因。

新板收到后仍要从第 1 节重新验收，不能因为文件名叫“稳定性修正版”就跳过上电测试。

---

## 10. OPA ON 和负载测试的最后门槛

在以下条件全部满足前，不允许接扬声器或长时间执行 OPA ON：

- TP8 观察 10 s 无持续 100–110 kHz；
- TP8 高频小于约 50 mVpp；
- TP7/TP8 直流工作点不贴轨；
- J1/J2 不进入 CC，正负轨无明显失衡；
- E/S 状态已经用 DMM 或足够带宽的差分测量记录；
- R5、R10、Cfb、临时线和探头接法均已写入记录。

推荐负载顺序：

```text
J4 开路 → 1 kΩ / ≥2 W → 20 Ω 功率电阻 → 7.2 Ω 功率电阻 → 实际扬声器
```

首次 OPA ON：

1. J1 设 ±4 V，每轨限流 25 mA；
2. J2 保持 ±10 V；
3. J4 先接 1 kΩ / ≥2 W 电阻；
4. TP8/TP16 和 TP2/TP17 同时观察；
5. OPA ON 只持续 20–50 ms；
6. 连续三次正常后才逐步延长；
7. 出现 TP2 高频 >1 Vpp、TP8 高频 >50 mVpp、限流或温升立即 OFF。

每更换一种负载，都要回到 J1 ±4 V、小信号、低限流重新开始。实际扬声器最后接，不能用扬声器承担第一次故障能量。

---

## 11. 怎样写实验记录，别人才能复现

### 11.1 每次实验的固定字段

建议复制下面模板作为 `board_record.md` 的一部分：

```markdown
# Board record

- Board ID:
- Board revision: V7 / V8 / UNKNOWN
- Date/time/timezone:
- Operator:
- PCB photos:
- U1 marking:
- U4 marking:
- U5 marking:
- R5/R10/R12/R15/R16/C27/Cfb state:
- J5 bridge:
- J7 fixture:
- Firmware artifact and SHA-256:
- OPTR before / after:
- UART settings:
- Probe type and bandwidth limit:
- Scope ground references:
- J1 setting/current limit/actual + and - current:
- J2 setting/current limit/actual + and - current:
- J3 setting/current:
- J4 load:
- OPA command state:
- DAC settings:
- TP7 Vpp/frequency/DC:
- TP8 Vpp/frequency/DC:
- TP9-TP14 value or differential waveform:
- Stop condition, if any:
- Result: PASS / CONDITIONAL / FAIL / EVIDENCE INCOMPLETE
- Next single variable:
```

### 11.2 波形文件命名

用能读懂的名字，不要只保存 `scope001.png`：

```text
C11-35-V8-20260915-A01__J1p4V_J2p10V__OPA_OFF__TP7_TP8__raw.csv
C11-35-V8-20260915-A01__J1p6V_J2p10V__OPA_OFF__TP7_TP8__105kHz.png
C11-35-V8-20260915-A01__R5_REMOVED__J1p4V__TP7_TP8__10s.csv
```

CSV、截图和日志要保留在同一实验目录；截图只能帮助人快速查看，CSV 才能支持频率、Vpp、相位和平均值的复核。

### 11.3 结论用四种状态

- **PASS：** 这个阶段的目标和判据满足；
- **CONDITIONAL：** 现象满足，但有明确的模型/仪器/覆盖范围限制；
- **FAIL：** 明确违反停止条件或功能判据；
- **EVIDENCE INCOMPLETE：** 发生了现象，但缺少同步波形、A-B-A 或可追溯原始数据，不能判根因。

例如：“OPA ON 后约 70 mA”应写成 `EVIDENCE INCOMPLETE` 或 `CONDITIONAL`，同时标明是电源显示电流还是瞬时峰值；不能只写“已证明 OPA548 自激”。

---

## 12. 一页式执行清单

### 收板

- [ ] 分配 Board ID，拍顶面/底面/U1/U4/J7 照片
- [ ] 判断 V7/V8；无法判断就标 UNKNOWN
- [ ] 对照 BOM、DNP、J5、J7
- [ ] 记录 R5、R10、R12、R15、R16、C27、Cfb

### 断电

- [ ] 拔掉 J1/J2/J3/J4，确认各轨低于 0.2 V
- [ ] 检查焊桥、方向、连接器和电源硬短路
- [ ] 预焊 TP7/TP8/TP16，必要时 TP9/TP14/TP11/TP12
- [ ] 确认示波器地夹位置，不接 TP8/TP12/TP14

### 数字域

- [ ] 只接 J3，5 V，50 mA
- [ ] J7 连接 V3V3/SWDIO/SWCLK/NRST/DGND
- [ ] SWD 1 MHz、connect-under-reset
- [ ] 读取并保存 OPTR；目标为 `0xFBEFF8AA`
- [ ] 擦除、写入、Verify、复位
- [ ] 连续 10 次烧录通过
- [ ] UART `PING/HELP/STATUS/READ ADC/STREAM`
- [ ] DAC1 观察 TP6，OPA 保持 OFF

### 模拟域

- [ ] J1 ±4 V、J2 先 ±8 V 再 ±10 V
- [ ] J4 开路、OPA OFF
- [ ] 同时保存 TP7/TP8
- [ ] 测 `TP9−TP14`，记录 E/S 裕量
- [ ] 没有持续 100–110 kHz 才能进入下一阶段

### 复现/改板

- [ ] 先复现 R10/E/S 风险，不把 V8 改回 V7
- [ ] TP8 异常时只做一次捕获并立即停机
- [ ] R5 拆除—恢复 A-B-A，至少两块板
- [ ] 必要时才做 R10 临时并联、Cfb 诊断、U4 换件
- [ ] 每次只改一个变量并恢复验证
- [ ] OPA ON 只用 1 kΩ 假负载、20–50 ms 脉冲开始
- [ ] 实际扬声器最后接

---

## 13. 项目内参考资料

- [V8 发布说明](KICAD部分/正弦电阻V8_KiCad10.0.4_稳定性修正版_2026-09-04/RELEASE_NOTES_V8.md)：V8 的器件、测试点、J7 和编程约束。
- [实板安全排障与根因判定计划](实际遇到的问题/实板安全排障与根因判定计划.md)：完整的电源限流、测试点坐标、R5 隔离和停机规则。
- [实板实验记录](实验还原部分/实板实验记录.md)：历史实测过程，以及哪些旧结论后来被收紧。
- [早期错误复现说明](实验还原部分/早期错误复现/README.md)：R10、错误示波器接地、OPA ON 高频负载和健康基线的 SPICE 复现。
- [最新证据复核](实验还原部分/主动审计/EVIDENCE_REASSESSMENT_20260915.md)：峰值与平均电流、较大启动扰动和当前根因边界。
- [H01–H12 假设总表](故障假设排查_20260908/假设总表.md)：单点错误及组合机制的当前状态。
- [V8 装配与验证输出](KICAD部分/正弦电阻V8_KiCad10.0.4_稳定性修正版_2026-09-04/release/V8_2026-09-04/)：BOM、CPL、Gerber、DRC/ERC 和 release 交叉检查。

本手册的核心判断只有一句话：**先把板子安全地变成“可测量、可复现、可回退”的对象，再谈哪个器件是根因。**
